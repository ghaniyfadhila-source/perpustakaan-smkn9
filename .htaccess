RewriteEngine On

# Prevent directory listing
Options -Indexes

# Handle requests - pass everything to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
